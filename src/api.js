const path = 'http://localhost:8081';

const api = {
  login: `${path}/login`,
  taobaoLogin: `${path}/loginTaoBao`,
  logout: `${path}/logout`,
  searchList: `${path}/searchList`,
  text: `${path}/searchList`,
};

export default api;
